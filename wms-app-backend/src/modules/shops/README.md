# shops

Allowlisted Shopify domains. Unknown installs must not create orders.

Public API:
- `normalizeShopDomain`
- `findByDomain`
- `isProcessable`
- `attachInstall` / `markUninstalled`
- `list` / `create` / `setEnabled`
